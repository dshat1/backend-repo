# mockingpets

This folder contains the mocks.router.js copied for convenience.
Routes available:
- GET /api/mocks/mockingpets
- GET /api/mocks/mockingusers
- POST /api/mocks/generateData

Note: The project uses existing models at src/dao/models/users.model.js and pets.model.js.