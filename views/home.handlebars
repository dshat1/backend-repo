<h2>Lista de Productos (Home)</h2>

{{#if products}}
    <ul id="productListHome">
        {{#each products}}
            <li>
                <strong>{{this.title}}</strong> - ${{this.price}}
                <p>{{this.description}}</p>
                <p>Categoría: {{this.category}}</p>
                <p>Stock: {{this.stock}}</p>
                <p>ID: {{this.id}}</p>
            </li>
        {{/each}}
    </ul>
{{else}}
    <p>No hay productos disponibles.</p>
{{/if}}